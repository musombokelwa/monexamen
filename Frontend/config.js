/* config.js — Generated at build time by Render build command.
   This file is replaced by the build script (build.sh) which injects
   the BACKEND_URL environment variable so the frontend knows where
   the API is hosted without hardcoding it.

   In local development this file is a no-op — window.BACKEND_URL
   stays undefined, and data.js falls back to localhost:5000.
*/
// window.BACKEND_URL is set by the build script below if BACKEND_URL env var exists.
// DO NOT manually edit this file — it is overwritten on every deploy.
