# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=monexamen
DB_USER=jenos
DB_PASSWORD=Api@12345

# Or use DATABASE_URL (Render provides this automatically)
# DATABASE_URL=postgresql://jenos:password@localhost:5432/monexamen

# Flask Configuration
SECRET_KEY=super-secret-key-pour-monexamen-jwt
FLASK_ENV=production

# Server Configuration
PORT=80
